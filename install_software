#clone MetaXcan github:
git clone https://github.com/hakyimlab/MetaXcan

#lots of python dependencies needed! python3, numpy, pandas, scipy, sqlalchemy, patsy, statsmodels, h5py
#instead of downloading all of these, can use a provided conda environment
#after installing conda or miniconda:
conda env create -f /path/to/this/repo/software/conda_env.yaml
conda activate imlabtools

#now ready to run any software tool in the MetaXcan package
